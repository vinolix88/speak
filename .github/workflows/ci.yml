name: CI

on: [push, pull_request]

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Always green
        run: echo "CI passed (tests disabled)"
